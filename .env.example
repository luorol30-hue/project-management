# Vercel Blob
# For an OIDC-connected Blob store, Vercel provides these automatically in the
# deployed function environment:
BLOB_STORE_ID=
VERCEL_OIDC_TOKEN=

# Legacy read/write token, only needed if your Blob connection is still token-based.
BLOB_READ_WRITE_TOKEN=
